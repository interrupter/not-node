const expect = require("chai").expect,
	HttpError = require('../src/error').Http,
	models = require("../src/manifest/models"),
	modulesPath = __dirname + '/modules';

describe("Models", function() {

	describe("importModel", function() {
		it("TODO", function() {
			expect(true).to.deep.equal(true);
		});
	});

	describe("importModelFrom", function() {
		it("TODO", function() {
			expect(true).to.deep.equal(true);
		});
	});

	describe("importModelsFrom", function() {
		it("TODO", function() {
			expect(true).to.deep.equal(true);
		});
	});

	describe("find", function() {
		it("TODO", function() {
			expect(true).to.deep.equal(true);
		});
	});

});
