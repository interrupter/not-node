
let replaceSets = {
	development: {},
	production: {},
	stage: {},
};

let babelOn = () => {
	return ['production', 'stage', 'development'].indexOf(process.env.NODE_ENV) > -1;
};

export {
	babelOn,
	replaceSets
};
